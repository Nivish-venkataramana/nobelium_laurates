# AegisQA dependencies — native Python 3.11+, no Docker required.

# Browser automation (accessibility-tree-first)
playwright>=1.47

# Groq inference (fast model + reasoning model)
groq>=0.11

# Dashboard UI
streamlit>=1.38

# Typed config (reads .env)
pydantic-settings>=2.5
python-dotenv>=1.0

# Demo target app (Phase 2)
flask>=3.0

# Dashboard backend / API (Phase 6)
fastapi>=0.115
uvicorn>=0.31

# CLI and console output
rich>=13.8
typer>=0.12

# Log watcher — file system tailing
watchdog>=4.0

# HTTP client (perf probe fallback)
httpx>=0.27

# Dashboard dataframes
pandas>=2.0
